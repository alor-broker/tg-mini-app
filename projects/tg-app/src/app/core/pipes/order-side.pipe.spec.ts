import { OrderSidePipe } from './order-side.pipe';

describe('OrderSidePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSidePipe();
    expect(pipe).toBeTruthy();
  });
});
